{
  "name": "video-chat-room-fora-soft",
  "private": true,
  "version": "0.1.0",
  "description": "Видеочат-комната до 4 участников: WebRTC (mesh) + Socket.io + React + Node.js",
  "workspaces": ["client", "server"],
  "engines": {
    "node": ">=20.0.0 <21.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "test": "npm run test -w server && npm run test -w client",
    "lint": "npm run lint -w server && npm run lint -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}