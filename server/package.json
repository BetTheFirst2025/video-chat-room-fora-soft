{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Signaling + chat server for video-chat-room",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "load": "artillery run load/room-join.yml --output load-report.json",
    "load:report": "artillery report load-report.json",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nanoid": "^5.0.7",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "artillery": "^2.0.21",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "socket.io-client": "^4.7.5",
    "supertest": "^7.0.0",
    "vitest": "^1.6.0"
  }
}
